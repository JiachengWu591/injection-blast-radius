# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets — never commit real credentials
.env

# Runtime artifacts (regenerated by demo scripts, not source)
sandbox/public_comments.txt
sandbox/*.log
sandbox/*.jsonl
