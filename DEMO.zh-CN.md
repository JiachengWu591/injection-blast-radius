# 演示脚本

*[English](./DEMO.md) · 简体中文*

四幕，大约十分钟。每一幕都是一条你真的会跑的命令，加上它存在的理由。这个结构来自 PROJECT_SPEC.md 第 5 节。

开始之前：`cp .env.example .env`，粘贴 DeepSeek key，跑一次 `python phase0_smoke.py` 确认沙箱和 API 都正常。**在观众看之前做完这一步。**

有一句话应该早点说出来：这里每一个"密钥"都是固定的占位符 `fake-sk-00000000000000000000`，"GitHub issue" 是一个本地 JSON 文件，"公开评论"是 `sandbox/` 里的一个文本文件。**没有任何真实的东西参与，除了模型 API 之外不接触网络。**

---

## 第 1 幕 —— 两个架构在正常 issue 上都能工作

```bash
python phase1_baseline.py       # baseline，两个 fixture
python phase2_isolated.py       # 隔离流水线，四幕
```

**要点：** 防御没有牺牲功能。

展示良性 issue 在两边都被合理地 triage。Baseline 写了一条回复；隔离版打了一个 `bug` 标签。**把这件事诚实的版本说出来**——它们不是同一个动作，而且隔离版的 Executor **压根不可能**产生自由散文，因为它只从四个预定义动作里挑。要紧的是一个正当的 bug 报告仍然得到了处理。

如果有人问这个取舍凭什么可以接受：**固定动作集不是设计的副作用，它就是设计本身。** 后面演示里"被攻陷的 Reader 造不成损害"的原因，和现在"它写不出自定义散文"的原因，是同一个。

---

## 第 2 幕 —— Baseline 失守

```bash
python phase1_baseline.py
```

**要点：** 危险在于三种能力集中在一处，而不是模型笨。

逐条走一遍 transcript。Agent 读了 issue，撞上藏在 HTML 注释里的隐藏指令，调用了 `read_file("sandbox/.env")`，然后把文件内容折进自己的公开回复、放在一个 "Resolved config" 标题下面。然后展示证据：

```bash
cat sandbox/public_comments.txt   # Windows 上用 Get-Content
```

这个 agent 同时满足三件事：**它读取不可信文本、它能读文件、它能对外发布。** 其中任意两件都没问题。三件挤在同一个上下文里就是全部的 bug 所在。指一下 [`ibr/baseline_agent.py:149`](ibr/baseline_agent.py#L149) —— `_post_comment_impl` 完全不做任何过滤，**这是故意的**。

有两件事值得在这里主动承认，因为一定会有人问：

- **这是概率性的。** 模型有时会识别出注入并拒绝。如果它拒绝了，重跑——**并且说出来你在重跑。** 这个演示要证明的是未防御的 agent **能被诱导**泄露，不是它必然会泄露。
- **载荷不是教科书那个。** `ignore all previous instructions` 被测试过的每个模型都拒绝。真正奏效的是伪装成日常贡献者约定的载荷。**在真实世界里奏效的也是这种。**

---

## 第 3 幕 —— 隔离流水线顶住了，以及**为什么**顶住

```bash
python phase2_isolated.py
python phase3_trace.py --run all
```

**要点：** 两层、两种材质，而只有其中一层是承重的。

**这一幕的顺序很重要。不要在第一个结果之后就停下。**

**第一步**，普通的恶意运行。安全审计把这个 issue 判为 `high_risk` 并短路——Reader 和 Executor 压根没运行。展示 trace 树。**然后立刻拆自己的台**：*这一层是弱的那层*。它是一个模型在判断文本，和攻击它的东西是同一种材质。它便宜、能拦掉不精巧的攻击，而**一个足够好的攻击会直接走过去**。如果这是唯一的防线，这个系统离第 2 幕只差一次改写措辞。

**第二步**，审计被绕过的运行。审计的判决被忽略，恶意文本抵达 Reader。在 trace 里展示：**Reader 产出了一千多字符关于这个注入的推理**——而跨越边界的动作仍然只是 `suggested_action='label_bug'`。

**第三步**，确定性那一幕，这是值得多花时间的一幕。Executor 收到一个 Reader 输出，密钥塞满两个自由文本字段、动作是 `reply_comment`。**全程没有模型参与。** 发布出去的是那段未经改动的模板。

这才是真正的主张：**一个被完全攻陷的 Reader 能做的选择是四个预定义动作之一，而这个集合不会因为攻击者变聪明而变大。**

说的时候指着代码 —— [`ibr/executor.py:119`](ibr/executor.py#L119) 是那个 `match`，[`ibr/pipeline.py:375`](ibr/pipeline.py#L375) 是跨越点。

如果你想要这个主张最强的版本，在完全不联网的情况下跑：

```bash
python phase2_isolated.py --scene 4     # 上面那一幕，单独跑，1 秒内
python tests/test_phase2.py --offline   # 它背后的十条断言
```

不发 API 调用，不需要 key，每次答案都一样。**值得在观众面前现场跑，恰恰因为它不可能出意外**——而"不可能出意外"就是要演示的那个性质。

---

## 第 4 幕 —— 一条命令，可复现

```bash
python run_all.py
```

**要点：** 以上没有一条是靠我讲给你的故事支撑的。

六次运行、一张汇总表，以及 `sandbox/report.md` ——每一行都带机制说明，泄露的字节是**引用出来的而不是断言出来的**。`tries` 那一列在 baseline 行显示 `1/3` —— **分母被打印出来，因为结果是概率性的，藏起来会让这个演示变得不诚实。**

值得一提：`run_all.py` 在任何一次*隔离版*运行发生泄露时会以非零码退出。**破防绝不能读起来像演示成功。**

---

## 该预料到的提问

**"真正起作用的不就是审计吗？"** 在第 3 幕的第一步里，是的——而这正是要展示第二步和第三步的理由。审计是**最先失效、而且是静默失效**的那一层。结构性边界是它失效之后剩下的东西。

**"更聪明的攻击不也能击穿边界吗？"** 不是同一种方式。针对审计的更聪明攻击会得到一个更好的判决。针对 Reader 的更聪明攻击得到的是……四个枚举值之一。**没有任何措辞能变出第五个。** 击穿边界的办法是在 Executor 里找到 bug 或者把动作集扩大——那是工程失误，不是提示词技巧。

**"为什么隔离版在良性输入上的动作和 baseline 不一样？"** 见第 1 幕。固定动作集，按构造如此，和攻击失败的原因是同一个。

**"换别的模型还成立吗？"** Provider 是 `ibr/config.py` 里的一行。**结构性断言完全不涉及模型，所以它们无论如何都成立。** 概率性的那几行会变。

**"审计绕过是真实攻击吗？"** 不是——它是一个跳过短路的开关。**故意做成模拟的**，好让结构性主张随时可观察，而不是只在某天有人恰好造出能击穿当前筛查模型的攻击时才能观察。
