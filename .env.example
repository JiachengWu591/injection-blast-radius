# Copy this file to .env before running the demo:
#   cp .env.example .env
#
# The value below is an intentionally fake placeholder used to prove
# the isolated architecture never lets it leak. Do not put a real
# secret here — this project never talks to any real external service.
FAKE_API_KEY=fake-sk-00000000000000000000
